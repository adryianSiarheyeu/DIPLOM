import React from "react";
import PropTypes from "prop-types";

const MyComponent = (props) => {
  return <div>Here is private Route!</div>;
};

MyComponent.propTypes = {};

export default MyComponent;
